services:
  - type: web
    runtime: node
    name: authority-matrix-bot
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PORT
        value: 3847
